const {execFile} = require('node:child_process')
const {fail} = require('./service-storage.cjs')
function externalTmux(binary='tmux',execute=execFile){
 // Keep format separators and Unicode paths intact without a shell's locale.
 function run(args,allowMissing=false){return new Promise((resolve,reject)=>{const env={...process.env};delete env.TMUX;execute(binary,['-u',...args],{env,encoding:'utf8',timeout:3000,maxBuffer:65536},(e,out,err)=>{if(!e)resolve(out);else if(allowMissing&&/no server running|No such file or directory/.test(err||''))resolve('');else reject(Object.assign(new Error('TMUX_FAILED'),{code:'TMUX_FAILED'}))})})}
 return {async create(name,cwd){if(!/^[A-Za-z0-9_-]{1,64}$/.test(name))fail('INVALID_SESSION_NAME');const target=(await run(['new-session','-d','-P','-F','#{session_id}','-s',name,'-c',cwd])).trim();if(!/^\$\d+$/.test(target))fail('TMUX_FAILED');return target},async discover(){const out=await run(['list-sessions','-F','#{session_id}\t#{session_name}\t#{session_attached}\t#{pid}\t#{session_created}\t#{session_windows}\t#{pane_current_path}'],true);return out.trim().split('\n').filter(Boolean).map(line=>{const [target,name,count,server,created,windows,cwd]=line.split('\t');if(!/^\$\d+$/.test(target))fail('TMUX_FAILED');return {target,name,attached:Number(count)>0,ownership:'external',identity:server+':'+created,windows:Number(windows)||1,cwd}})},async inspect(target){return (await this.discover()).find(s=>s.target===target)||null},async write(target,data){if(!/^\$\d+$/.test(target))fail('INVALID_REQUEST');return run(['send-keys','-t',target,'-H',...Array.from(Buffer.from(data),v=>v.toString(16).padStart(2,'0'))])}}
}
module.exports={externalTmux}
