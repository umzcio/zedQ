const {test}=require('node:test');const assert=require('node:assert/strict');
const {allowVoicePermission}=require('../electron/voice-permissions.cjs');
const contents={isDestroyed:()=>false,getURL:()=> 'file:///app/index.html',mainFrame:{url:'file:///app/index.html'}};
const request={webContents:contents,trustedWebContents:contents,trustedUrl:'file:///app/index.html',active:{phase:'recording'},permission:'media',details:{isMainFrame:true,requestingUrl:'file:///app/index.html',mediaTypes:['audio']},kind:'request'};
test('only an active trusted main-frame audio request is allowed',()=>{assert.equal(allowVoicePermission(request),true);for(const patch of [{active:null},{active:{phase:'transcribing'}},{webContents:{}},{permission:'display-capture'},{details:{...request.details,isMainFrame:false}},{details:{...request.details,requestingUrl:'https://example.com'}},{details:{...request.details,mediaTypes:['audio','video']}},{details:{...request.details,mediaTypes:[]}},{trustedUrl:'file:///different/index.html'}])assert.equal(allowVoicePermission({...request,...patch}),false)});
test('permission checks require audio and exact frame, not origin-only trust',()=>{const check={...request,kind:'check',details:{isMainFrame:true,requestingUrl:'file:///app/index.html',mediaType:'audio'}};assert.equal(allowVoicePermission(check),true);assert.equal(allowVoicePermission({...check,details:{...check.details,mediaType:'video'}}),false);assert.equal(allowVoicePermission({...check,details:{...check.details,requestingUrl:undefined}}),false)});
